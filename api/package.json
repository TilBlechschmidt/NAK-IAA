{
  "license": "MIT",
  "scripts": {
    "docs": "spot docs specification/api.ts",
    "validate": "spot validate specification/api.ts",
    "mock": "spot mock specification/api.ts",
    "lint": "spot lint specification/api.ts",
    "generate": "spot generate -c specification/api.ts -g openapi3 -l yaml -o target/",
    "generate-docs": "yarn generate && redoc-cli bundle target/api.yml -o target/api.html --title 'API Documentation' --options.pathInMiddlePanel=true"
  },
  "dependencies": {
    "@airtasker/spot": "^1.2.0",
    "@openapitools/openapi-generator-cli": "^2.0.3",
    "redoc-cli": "^0.9.12"
  }
}
